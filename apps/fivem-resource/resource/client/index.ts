// The loading screen is served as a vanilla HTML/CSS/JS page from `public/`
// (`loadscreen 'dist/load.html'`). No client script is required, so this entry
// is intentionally empty — reserved for NUI lifecycle work if needed later.
export {};
